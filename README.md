# bitsgap-test
# Bitsgap QA Automation Test

## 📋 Описание
Проект включает в себя комплексный тест, который проверяет полный цикл торговой операции:

логин в систему

Настройка демо-режима

Выбор типа ордера (Limit)

Установка параметров ордера 

Размещение ордера через интерфейс

## 🛠 Технологии
Playwright - фреймворк для автоматизации тестирования

TypeScript - язык программирования

## ⚙️ Установка и настройка
Клонирование репозитория:

git clone https://github.com/Arinka75/bitsgap-test.git

cd bitsgap-test

Установка зависимостей

npm install

Установка Playwright браузеров

npx playwright install

## 🚀 Как запускать

Запуск всех тестов

npm test

Запуск в отладочном режиме

npx playwright test --debug

## 🔐 Авторизация в тестах: Storage State vs Login Flow
Выбранный подход: Storage State
В проекте используется подход с сохранением состояния аутентификации через storageState.json вместо выполнения полного flow логина в каждом тесте.

## 📊 Сравнение подходов
Критерий	     Storage State	             Login Flow

Скорость	    ⚡️ Быстро (миллисекунды)	🐢 Медленно (секунды)

Надежность	    ✅ Высокая	               ❌ Зависит от UI/сети

Стабильность	✅ Стабильный	           ❌ Хрупкий (селекторы, UI)

Тестовые данные	🔄 Требует обновления	    ✅ Всегда актуальные

Сложность	    🟢 Просто	                🔴 Сложно

## Комментарий к работе

В данной тестовой работе я, к сожалению, не смогла реализовать перехват WebSocket, так как не увидела ни одного в devtools и не смогла реализовать проверку в таблице Orders, так как после нажатия кнопки "Buy BTC" запросы не отправлялись на бек, возможно, из за этого не прогружалась строка в таблицу. 
Но если есть возможность, я была бы рада услышать обратную связь по тому, как это можно было реализовать.

Также, хочу расписать изменения, которые я бы внесла в код, если бы не ограниченность во времени.

1. Переделала бы структуру проекта, добавив базовые классы и наследование от них.

2. Вынесла бы тестовые файлы в отдельный файл, чтобы рефакторинг тестов был более быстрым и прозрачным.

3. Pages разделила бы на компаненты.
